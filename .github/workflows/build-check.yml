name: Build check

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: source
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Use Node.js 24
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: source/package-lock.json

      - name: Inspect last valid integrations mount
        shell: bash
        run: |
          git show 3ae33c3d38c5189dd2ce377fcdfa6670fb42931e:source/src/App.tsx > /tmp/App.valid.tsx
          echo "--- settings navigation ---"
          sed -n '22540,22620p' /tmp/App.valid.tsx
          echo "--- integrations start ---"
          sed -n '22990,23130p' /tmp/App.valid.tsx
          echo "--- integrations end ---"
          sed -n '23640,23740p' /tmp/App.valid.tsx

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

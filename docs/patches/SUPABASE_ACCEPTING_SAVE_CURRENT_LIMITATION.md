# Current limitation

The branch currently fixes the Supabase schema side and documents the code fallback.

The code fallback is not yet applied because it requires editing existing large function files, not just adding a new file.

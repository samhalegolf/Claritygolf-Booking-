# Do not merge yet

This branch should not be treated as complete until the defensive code fallback is applied and `npm run build` passes.

The production SQL file can still be used immediately to unblock the live lesson-save failure.
